package com.lany192.box.demo.ui.guide

import org.libpag.PAGImageView
import org.libpag.PAGImageView.PAGImageViewListener

open class OnPagAnimationListener : PAGImageViewListener {
    override fun onAnimationStart(pagImageView: PAGImageView) {
    }

    override fun onAnimationEnd(pagImageView: PAGImageView) {
    }

    override fun onAnimationCancel(pagImageView: PAGImageView) {
    }

    override fun onAnimationRepeat(pagImageView: PAGImageView) {
    }

    override fun onAnimationUpdate(pagImageView: PAGImageView) {
    }
}
